# My Cloud — Облачное файловое хранилище

Дипломный проект по профессии **«Fullstack-разработчик на Python»** от образовательной платформы Netology.

`My Cloud` — это современное полнофункциональное веб-приложение (Single Page Application) для персонального облачного хранения и совместного использования файлов. Проект представляет собой полноценный Fullstack продукт с мощным REST API на Django и интерактивным интерфейсом на React + TypeScript с премиальным Glassmorphism дизайном.

---

## ⚡ Основные возможности проекта

### 🧑‍💻 Для авторизованных пользователей
* **Регистрация и авторизация**: безопасная сессионная аутентификация с проверкой пароля и CSRF-защитой.
* **Интерактивное хранилище (Файловый менеджер)**:
  * Загрузка файлов любого типа с помощью интуитивного интерфейса **Drag-and-Drop** или стандартного проводника.
  * Возможность добавлять необязательные текстовые комментарии к загружаемым файлам.
  * Скачивание файлов с автоматической фиксацией даты последнего обращения.
  * Inline-редактирование (переименование файлов и изменение комментариев) без перезагрузки страниц.
  * Удаление файлов из базы данных и с диска.
* **Публичный доступ (Шеринг)**:
  * Генерация уникальной зашифрованной публичной ссылки для любого файла в один клик.
  * Автоматическое копирование ссылки в буфер обмена.
  * Специальный публичный роут для скачивания файлов гостями системы (без необходимости авторизации).

### 👑 Для администраторов (Admin-панель)
* **Статистика пользователей**: вывод списка всех зарегистрированных пользователей системы с указанием ID, имени, email, роли, общего количества загруженных файлов и суммарного занимаемого места на диске (с автоматическим форматированием размера в B/KB/MB/GB).
* **Управление ролями**: переключение прав администратора (`is_staff`) по клику на бейдж пользователя с защитой от лишения прав самого себя.
* **Удаление учетных записей**: полное удаление пользователей и всех связанных с ними файлов с физического диска сервера (с защитой от удаления самого себя).
* **Просмотр чужих хранилищ**: возможность открыть список файлов любого пользователя и управлять ими (скачивать или удалять от лица администратора) с пересчетом общего объема занятого места в реальном времени.

---

## 🛠️ Стек технологий

### 🛡️ Backend
* **Python 3.12+**
* **Django 6** — веб-фреймворк.
* **Django REST Framework (DRF)** — построение RESTful API.
* **PostgreSQL** — основная реляционная база данных для хранения метаданных и связей.
* **python-decouple** — безопасная конфигурация через переменные окружения (`.env`).
* **Django Test Framework** — юнит- и интеграционное тестирование API.

### 🎨 Frontend
* **React 19**
* **TypeScript 6** — строгая типизация интерфейса.
* **Vite 8** — быстрый инструмент сборки и среда разработки.
* **Redux Toolkit** — централизованное управление состоянием приложения (`authSlice`, `filesSlice`, `adminSlice`).
* **React Router 7** — организация роутинга и защищенных маршрутов (Protected Routes).
* **CSS Modules (Vanilla CSS)** — стилизация компонентов с изоляцией классов, эффектом матового стекла (Glassmorphism), градиентами и плавными микро-анимациями.

---

## 📂 Структура проекта

```text
.
├── backend/                  # Серверная часть (Django + DRF)
│   ├── apps/
│   │   ├── users/            # Модуль пользователей (регистрация, сессии, администрирование)
│   │   │   ├── api.py
│   │   │   ├── models.py
│   │   │   ├── serializers.py
│   │   │   ├── services.py
│   │   │   └── tests/
│   │   └── files/            # Модуль файлов (хранение, скачивание, шеринг, статистика)
│   │       ├── api.py
│   │       ├── models.py
│   │       ├── public_api.py
│   │       ├── serializers.py
│   │       ├── services.py
│   │       └── tests/
│   ├── common/               # Вспомогательные утилиты, права доступа и кастомные ошибки
│   ├── config/               # Настройки проекта Django (settings, urls, wsgi)
│   ├── storage/              # Директории статики (static) и загружаемых медиафайлов (media)
│   ├── .env.example          # Пример файла окружения для бэкенда
│   ├── manage.py
│   └── requirements.txt      # Зависимости Python
├── frontend/                 # Клиентская часть (React + TS + Redux)
│   ├── src/
│   │   ├── components/       # Общие переиспользуемые UI-компоненты (Модальные окна, Спиннеры, Ошибки)
│   │   ├── pages/            # Страницы (Авторизация, Личный кабинет/Хранилище, Админка)
│   │   ├── services/         # Настройка Axios инстанса и API-запросов
│   │   ├── store/            # Redux Стор и срезы состояния (slices)
│   │   ├── utils/            # Утилиты форматирования
│   │   ├── App.tsx           # Корневой компонент с маршрутами
│   │   └── main.tsx          # Точка входа в React приложение
│   ├── index.html
│   ├── package.json          # Зависимости и скрипты фронтенда
│   ├── tsconfig.json         # Настройки компилятора TypeScript
│   └── vite.config.ts        # Конфигурация Vite
└── README.md
```

---

## 🚀 Инструкция по локальному запуску

### 1. Клонирование репозитория
```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd <ИМЯ_ПАПКИ>
```

### 2. Настройка и запуск Backend

1. **Перейдите в директорию backend**:
   ```bash
   cd backend
   ```
2. **Создайте виртуальное окружение**:
   * *Windows*:
     ```bash
     python -m venv .venv
     .venv\Scripts\Activate.ps1
     ```
   * *Linux / macOS*:
     ```bash
     python3 -m venv .venv
     source .venv/bin/activate
     ```
3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```
4. **Создайте файл конфигурации `.env`**:
   Скопируйте пример окружения и заполните его своими доступами:
   * *Windows*:
     ```bash
     copy .env.example .env
     ```
   * *Linux / macOS*:
     ```bash
     cp .env.example .env
     ```
   Пример конфигурации:
   ```env
   SECRET_KEY=django-insecure-key
   DEBUG=True
   ALLOWED_HOSTS=127.0.0.1,localhost
   DB_NAME=cloud_storage
   DB_USER=postgres
   DB_PASSWORD=your_password
   DB_HOST=127.0.0.1
   DB_PORT=5432
   ```
5. **Создайте базу данных в PostgreSQL** (например, с именем `cloud_storage`), запустите СУБД и проверьте подключение.
6. **Примените миграции**:
   ```bash
   python manage.py migrate
   ```
7. **Создайте учетную запись Администратора**:
   ```bash
   python manage.py createsuperuser
   ```
8. **Запустите сервер разработки Django**:
   ```bash
   python manage.py runserver
   ```
   Сервер бэкенда будет доступен по адресу `http://127.0.0.1:8000/`.

---

### 3. Настройка и запуск Frontend

1. **Перейдите в директорию frontend**:
   Откройте новую вкладку терминала и выполните:
   ```bash
   cd frontend
   ```
2. **Установите зависимости Node.js** (рекомендуется использовать `npm` или `yarn`):
   ```bash
   npm install
   # или с помощью yarn
   yarn install
   ```
3. **Запустите сервер разработки Vite**:
   ```bash
   npm run dev
   # или с помощью yarn
   yarn dev
   ```
   Приложение будет доступно по адресу `http://localhost:5173/` (или по порту, который выведет Vite в консоль).

---

## 🧪 Запуск тестов бэкенда

Тесты покрывают права доступа, авторизацию, логирование, операции с файлами и очистку физического хранилища. Запускайте тесты из директории `backend`:

* **Запуск всех тестов**:
  ```bash
  python manage.py test
  ```
* **Запуск тестов пользователей**:
  ```bash
  python manage.py test apps.users.tests
  ```
* **Запуск тестов файловых операций**:
  ```bash
  python manage.py test apps.files.tests
  ```

---

## 📡 Основные API-эндпоинты бэкенда

### Авторизация и пользователи (`/api/users/`)
* `POST /api/users/register/` — регистрация нового аккаунта.
* `POST /api/users/login/` — авторизация пользователя (устанавливает сессионную куку).
* `POST /api/users/logout/` — выход из системы с инвалидацией сессии.
* `GET /api/users/me/` — получение данных текущего авторизованного пользователя.
* `GET /api/users/` — получение списка всех пользователей со статистикой *(доступно только Администратору)*.
* `PATCH /api/users/<user_id>/` — изменение статуса прав администратора `is_staff` *(доступно только Администратору)*.
* `DELETE /api/users/<user_id>/` — полное удаление пользователя и всех его файлов *(доступно только Администратору)*.

### Управление файлами (`/api/files/`)
* `GET /api/files/` — получить список файлов. Обычный пользователь видит только свои файлы. Администратор может передать параметр `?owner_id=<user_id>` для просмотра файлов любого конкретного пользователя.
* `POST /api/files/` — загрузка нового файла на сервер с опциональным комментарием (передается в формате `multipart/form-data`).
* `GET /api/files/<file_id>/` — детальная информация о файле.
* `PATCH /api/files/<file_id>/` — переименование файла или обновление его комментария.
* `DELETE /api/files/<file_id>/` — удаление файла из БД и физического хранилища.
* `GET /api/files/<file_id>/download/` — скачивание файла пользователем/администратором (записывает время скачивания).
* `POST /api/files/<file_id>/public-link/` — генерация или получение существующего публичного токена доступа к файлу.

### Публичный доступ (`/api/public/`)
* `GET /api/public/files/<token>/` — скачивание файла сторонним пользователем по сгенерированному публичному токену (не требует авторизации в системе).

---

## 🔒 Безопасность и архитектурные особенности

* **Изоляция медиафайлов при тестах**: тесты модуля `files` используют временные переопределяемые директории для хранения тестовых файлов, что гарантирует чистоту основного `MEDIA_ROOT`.
* **Валидация на фронтенде и бэкенде**: формы регистрации, авторизации и переименования проходят многоуровневую проверку (на длину полей, формат почты, пароль, наличие спецсимволов).
* **Стеклянный интерфейс (Glassmorphism)**: все модальные окна, списки и панели используют CSS-свойство `backdrop-filter: blur(...)` для создания стильного полупрозрачного размытия в сочетании с плавной сменой состояний.

---

## 🛠️ Выполнение замечаний преподавателя (Александра Мужева)

Все замечания, полученные на этапе проверки бэкенда, были полностью устранены:
1. **Просмотр файлов в браузере (Inline-просмотр)**:
   * На бэкенде реализована поддержка query-параметра `?inline=true` для эндпоинта скачивания файлов, отключающая принудительное скачивание (`as_attachment=False`) и возвращающая файл с заголовком `Content-Disposition: inline`.
   * На фронтенде добавлена кнопка **«Просмотр»** (иконка глаза), которая открывает картинки, PDF и текстовые файлы непосредственно в новой вкладке браузера без принудительной загрузки на диск.
2. **Форматирование размера хранилища (`total_size`)**:
   * На фронтенде реализован хелпер, конвертирующий байты в понятные человеку величины (Байты, КБ, МБ, ГБ). В таблице администратора размер хранилища каждого пользователя и размеры файлов выводятся в отформатированном виде (например, `1.33 МБ`).
3. **Удаление пустых файлов**:
   * Файлы `backend/common/exceptions.py` и `backend/common/utils.py` были удалены из структуры проекта как неиспользуемые.
4. **Сквозная интеграция**:
   * Разработан полнофункциональный React (TypeScript, Redux Toolkit, React Router) SPA клиент, полностью взаимодействующий с Django API.

---

## 🌐 Деплой на продакшн-сервере (VPS)

Приложение успешно развернуто и доступно в продакшене по постоянному адресу:
* **Публичная ссылка на проект**: [http://80.78.254.193/](http://80.78.254.193/)
* **Тестовый административный аккаунт**:
  * **Логин**: `cloud_user`
  * **Пароль**: `Dem_Qwerty`

### Схема развертывания
Приложение развернуто на виртуальном сервере (Ubuntu 24.04 LTS) под управлением **Nginx** и **Gunicorn**:
* **Nginx** работает как обратный прокси (Reverse Proxy) для Django (Gunicorn) и напрямую раздает статические файлы собранного React-приложения (`frontend/dist`) и медиафайлы из `storage/media/`.
* **Gunicorn** запущен в виде системного демона `systemd` и прослушивает локальный порт `127.0.0.1:8000` для обработки API-запросов.
* В качестве СУБД используется **PostgreSQL**, развернутый локально на сервере.

### Основные этапы настройки сервера:
1. **Настройка Swap**: так как сервер имеет 1 ГБ оперативной памяти, был создан swap-файл объемом 2 ГБ для предотвращения падения процессов из-за нехватки памяти (OOM) во время сборки фронтенда.
2. **Настройка Gunicorn (`/etc/systemd/system/gunicorn.service`)**:
   ```ini
   [Unit]
   Description=gunicorn daemon for My Cloud backend
   After=network.target

   [Service]
   User=root
   WorkingDirectory=/root/netology_78_cloud-file-storage-graduation-project/backend
   ExecStart=/root/netology_78_cloud-file-storage-graduation-project/backend/.venv/bin/gunicorn \
             --workers 3 \
             --bind 127.0.0.1:8000 \
             config.wsgi:application

   [Install]
   WantedBy=multi-user.target
   ```
3. **Конфигурация Nginx (`/etc/nginx/sites-available/default`)**:
   ```nginx
   server {
       listen 80 default_server;
       listen [::]:80 default_server;

       server_name 80.78.254.193;
       client_max_body_size 500M;

       # 1. Раздача фронтенда (React SPA)
       location / {
           root /root/netology_78_cloud-file-storage-graduation-project/frontend/dist;
           index index.html;
           try_files $uri $uri/ /index.html;
       }

       # 2. Раздача медиафайлов
       location /media/ {
           alias /root/netology_78_cloud-file-storage-graduation-project/backend/storage/media/;
       }

       # 3. Раздача статики бэкенда (админка Django)
       location /static/ {
           alias /root/netology_78_cloud-file-storage-graduation-project/backend/storage/static/;
       }

       # 4. Проксирование API-запросов к Django
       location /api/ {
           proxy_set_header Host $http_host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
           proxy_pass http://127.0.0.1:8000;
       }
   }
   ```
4. **Настройка прав доступа**: для обеспечения доступа Nginx (`www-data`) к файлам сборки в `/root` были выполнены команды:
   ```bash
   chmod +x /root
   chmod -R o+rx /root/netology_78_cloud-file-storage-graduation-project
   ```

---

## Автор

Дмитрий Морозов

- ![LinkedIn](./svg/linkedin-icon.svg) [LinkedIn](https://www.linkedin.com/in/dm-morozov/)
- ![Telegram](./svg/telegram.svg) [Telegram](https://t.me/dem2014)
- ![GitHub](./svg/github-icon.svg) [GitHub](https://github.com/dm-morozov/)
- ![LeetCode](./svg/leetcode-icon.svg) [LeetCode](https://leetcode.com/u/dm-morozov/) — задачи для подготовки к собеседованиям
- ![Codewars](./svg/codewars-icon.svg) [Codewars](https://www.codewars.com/users/dm-morozov) — ежедневная практика и тренировка

---

## Лицензия

Учебный проект в рамках дипломной работы Netology.
