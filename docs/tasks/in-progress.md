# Задачи в работе

- [ ] Проектирование и реализация модуля `files`
  Контекст: `users` доведён до состояния рабочего MVP auth-блока; перед началом кода по `files` нужно держать в памяти остаточные задачи по `users` и не потерять их.

Когда начинаешь задачу, переноси её сюда в формате:

- [ ] Краткое название
  Контекст: что именно делается и какой ожидается результат.
