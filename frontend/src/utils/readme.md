# utils/ (Утилиты)

Простые вспомогательные функции, которые не связаны с React.

Например, бэкенд отдает размер файла в байтах (1048576).
Пользователю это читать неудобно. Мы напишем утилиту formatBytes(bytes), которая превратит это в 1 МБ.
